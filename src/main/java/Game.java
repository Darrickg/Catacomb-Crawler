/*
public class Game {

    public void gameLoop() {
        while (true) {
            // handle user input
            if (GameKeyListener.isUpPressed()) {
                Player.moveUp();
            } else if (GameKeyListener.isDownPressed()) {
                Player.moveDown();
            } else {
                Player.stopY();
            }
            if (GameKeyListener.isLeftPressed()) {
                Player.moveLeft();
            } else if (GameKeyListener.isRightPressed()) {
                Player.moveRight();
            } else {
                Player.stopX();
            }
            // TODO: handle collisions


            // update game objects
            Player.move();


            // render the game


        }
    }

}
*/